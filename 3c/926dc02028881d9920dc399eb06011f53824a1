Timeline
- User request (through the coordinator): a T-legality gate check over all
  14 traces, negative-tested with an illegal-edge mutant; the schema gate
  before the state gate.
- Traces carry digests, not phases, so the phase log has to come from the
  drivers: the driver protocol gains "phases" (one per frame). Traces and
  their hash are unchanged.
- The mutant is a lying driver (engine right, phase log doctored) so that
  the rejection can only come from the state gate, not from the oracle.
- Gate run on this tree: corrupted trace rejected by both drivers, 6
  mutants rejected (incl. illegal-edge-gameover-playing), hand-edited
  Appendix A digest rejected, appendix PASS, python 14/14, hy 14/14,
  GATE: PASS. Same run: hy pytest 79 passed, python pytest 78 passed.

Reproduction
  nice -n 10 lockf -k -t 7200 /scratch/locks/heavy.lock sh -c \
    'bin/verify.sh; python -m pytest impl/hy/tests -q; python -m pytest impl/python/tests -q'
  -> self-test ok x9; appendix PASS; python PASS 14/14; hy PASS 14/14;
     GATE: PASS; 79 passed; 78 passed
  trace set -> 981baab4279c5c9f93f42eda23f1eb9f2b0889b4c228637e2ac19b9a479c2026
Supported cell: FreeBSD 15.1-RELEASE amd64, Python 3.12.14, hy 1.3.1,
  hypothesis 6.165.10, pytest 9.1.1 (venv /scratch/venvs/tetris-py).

Addendum: the illegal-edge mutant is rejected by the state gate alone
  nice -n 10 lockf -k -t 7200 /scratch/locks/heavy.lock python spec/conformance/run.py --verbose \
    --impl "env PYTHONPATH=impl/python/engine python impl/python/conformance/mutants.py illegal-edge-gameover-playing"
  -> ok 01..11 (no game over to doctor); FAIL 12, 13, 14:
     FINDING 12-game-over-reset: illegal phase edge gameover -> playing into frame 317
     FINDING 13-suspended-input: illegal phase edge gameover -> playing into frame 331
     FINDING 14-bot-marathon: illegal phase edge gameover -> playing into frame 2738
     FAIL: 3 finding(s) over 14 traces (no digest or final finding: the oracle agreed)
