Timeline
- Wrote the cljc engine from SPEC v2 (9a73f82), then the driver. The first
  runner run passed 14/14 before any spec change was considered.
- Added the clojure token and driver() case to bin/verify.sh (last line of
  each, per /scratch/work/tetris-parallel/OWNERSHIP.md).
- Fast-forwarded to the v2 seal (61e2e83); bin/verify.sh was unchanged there.

Reproduction
  nice -n 10 lockf -k -t 7200 /scratch/locks/heavy.lock timeout 900 bin/verify.sh
  self-test ok: corrupted trace rejected (python driver)
  self-test ok: corrupted trace rejected (hy driver)
  self-test ok: corrupted trace rejected (clojure driver)
  self-test ok: mutant 'ccw-release-keeps-dcd' rejected
  self-test ok: mutant 'gravity-ceil-cadence' rejected
  self-test ok: mutant 'level-target-plus-six' rejected
  self-test ok: mutant 'standard-180-kicks' rejected
  self-test ok: mutant 'no-ghost' rejected
  self-test ok: mutant 'illegal-edge-gameover-playing' rejected
  self-test ok: hand-edited Appendix A digest rejected
  PASS: SPEC Appendix A matches the traces
  == python / == hy / == clojure
  PASS: 14/14 traces, trace-set sha256 981baab4279c5c9f93f42eda23f1eb9f2b0889b4c228637e2ac19b9a479c2026 (each)
  GATE: PASS

Supported cell
  FreeBSD 15.1-RELEASE amd64; babashka 1.13.220 (driver); Python 3.12.14
  venv /scratch/venvs/tetris-py (runner, python and hy drivers).
